# Backend

FastAPI service for repository analysis and AI orchestration.

## Initial modules

- `app/main.py` — API entry point
- `app/core/` — configuration and shared infrastructure
- `app/api/` — HTTP routes
- `app/services/` — repository and analysis services
- `tests/` — backend tests
